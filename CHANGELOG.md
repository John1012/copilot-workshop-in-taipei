# 變更紀錄

## [0.1.0] - Step 1: 基本的新增、完成、刪除、未完成計數、localStorage 保存

### 新增

- 新增待辦項目輸入欄位與新增按鈕。
- 新增待辦事項至清單中，支援直接輸入新增。
- 新增完成狀態勾選，讓使用者可以標記任務已完成。
- 新增刪除按鈕，允許移除單一待辦項目。
- 新增未完成項目計數，顯示目前還有幾項待辦未完成。
- 新增 localStorage 持久化儲存，讓頁面重新整理後仍保留待辦資料。

### 變更

- 調整待辦清單的基本渲染邏輯，讓新增、勾選與刪除後可即時更新畫面。
- 更新未完成計數的計算方式，確保顯示的是目前尚未完成的數量。
- 優化資料讀取與保存流程，避免資料在重新整理後遺失。

## [0.2.0] - Step 2: 深色模式切換(含記住偏好與跟隨系統設定)、全部/未完成/已完成篩選

### 新增

- 新增深色模式切換按鈕，位於標題列右側。
- 新增主題切換圖示與文字提示，顯示為「🌙 深色模式」或「☀️ 淺色模式」。
- 新增深色/淺色主題樣式，讓待辦清單支援明亮與暗黑兩種視覺模式。
- 新增全部、未完成、已完成三種篩選按鈕。
- 新增篩選後空白狀態提示，當符合條件的項目為空時顯示對應訊息。
- 新增使用者主題偏好儲存，讓重新整理後維持上一個主題選擇。
- 新增系統主題偵測邏輯，若使用者尚未手動選擇主題，則跟隨作業系統的深淺色設定。

### 變更

- 調整 CSS 變數結構，將淺色與深色色票統一納入主題變數，避免各處散落色碼。
- 更新待辦清單的渲染邏輯，讓篩選狀態與資料顯示同步。
- 優化底部未完成計數，確保其數字始終反映整體未完成項目，不受篩選條件影響。
- 更新選中篩選按鈕的樣式，讓目前篩選狀態更直觀明確。
